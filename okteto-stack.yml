services:
  drone-vc-p:
    build: .
    environment:
      BOT_TOKEN: $BOT_TOKEN
      PROTECT_CONTENT: $PROTECT_CONTENT
      DISABLE_NOTIFICATION: $DISABLE_NOTIFICATION
    resources:
      cpu: 1000m
      memory: 1Gi